# # This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# # For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# # This workflow uses actions that are not certified by GitHub.
# # They are provided by a third-party and are governed by
# # separate terms of service, privacy policy, and support
# # documentation.

# name: Integration tests

# on:
#   push:
#     branches: [ "master" ]

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v3
#     - name: Set up JDK 17
#       uses: actions/setup-java@v3
#       with:
#         java-version: '17'
#         distribution: 'temurin'
#         cache: maven
#     - name: Build tests with Maven
#       env:
#        usos_baseurl: ${{ secrets.USOS_BASEURL }}
#        usos_consumerKey: ${{ secrets.USOS_CONSUMERKEY }}
#        usos_consumerSecret: ${{ secrets.USOS_CONSUMERSECRET }}
#        usos_login: ${{ secrets.USOS_LOGIN }}
#        usos_password: ${{ secrets.USOS_PASSWORD }}
#       run: cd core && mvn -B install --file pom.xml && cd .. && cd debug && mvn -B test --file pom.xml
